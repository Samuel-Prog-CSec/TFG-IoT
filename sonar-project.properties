sonar.projectKey=Samuel-Prog-CSec_TFG-IoT
sonar.organization=samuel-github-projects


# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=TFG-IoT
sonar.projectVersion=0.2.0


# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Nota: este repo incluye firmware en C++ (rfid_scanner). SonarCloud (CFamily) requiere
# compile_commands.json (sonar.cfamily.compile-commands) para analizar C/C++ en modo manual.
# Como este proyecto usa principalmente JS/TS, excluimos C/C++ del análisis para evitar fallos.
sonar.sources=.

# Excluir carpetas que no deben analizarse (o que generan mucho ruido)
sonar.exclusions=**/node_modules/**,**/coverage/**,**/dist/**,**/build/**,**/.scannerwork/**,rfid_scanner/**

# Desactivar análisis C/C++/ObjC (evita el sensor CFamily y el requisito compile_commands)
sonar.c.file.suffixes=-
sonar.cpp.file.suffixes=-
sonar.objc.file.suffixes=-

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8